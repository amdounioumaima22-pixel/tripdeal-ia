services:
  - type: web
    name: tripdeal-ia
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api.app:app
    envVars:
      - key: GROQ_API_KEY
        sync: false